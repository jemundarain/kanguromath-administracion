export class Submit {

    public constructor(public _id: string, public test: string, public mode: string, public difficulty: number, public in_progress: boolean, public t_remain_s: number, public score: number){ }

}